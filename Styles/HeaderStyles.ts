import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  HomeScreen: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  heading: {
    paddingBottom: 20,
    fontSize: 25,
  },
});
